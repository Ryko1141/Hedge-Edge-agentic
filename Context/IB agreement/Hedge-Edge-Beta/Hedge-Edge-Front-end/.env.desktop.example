# HedgeEdge Desktop Environment Variables
# Copy this file to .env and fill in your values before building

# =============================================================================
# OPTIONAL: Supabase Configuration (for cloud sync/auth)
# =============================================================================
# Leave these blank or remove to run in local-only mode
# Your Supabase project URL (found in Project Settings > API)
# VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase public/anon key (safe to include in client apps)
# VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key

# =============================================================================
# METAAPI CLOUD CONNECTION (Optional)
# =============================================================================
# MetaAPI token for cloud-based MT4/MT5 provisioning.
# NOT prefixed with VITE_ — only available in the Electron main process.
# METAAPI_TOKEN=your-metaapi-token

# =============================================================================
# TRADING AGENT CONFIGURATION (Optional)
# =============================================================================
# By default, the app uses bundled agents if available.
# Set these to override with external agents (e.g., running on a VPS).

# Default ports:
# - MT5 Agent: 5101
# - cTrader Agent: 5102

# Force agent mode for all platforms: 'bundled' or 'external'
# HEDGEEDGE_AGENT_MODE=bundled

# =============================================================================
# LICENSE API CONFIGURATION
# =============================================================================
# Railway backend URL (default: production). Desktop app NEVER calls Creem directly.
# HEDGE_EDGE_LICENSE_API_URL=https://hedgeedge-railway-backend-production.up.railway.app

# MT5 Agent Configuration
# HEDGEEDGE_MT5_AGENT_HOST=127.0.0.1
# HEDGEEDGE_MT5_AGENT_PORT=5101
# HEDGEEDGE_MT5_AGENT_PATH=/path/to/mt5-agent

# cTrader Agent Configuration
# HEDGEEDGE_CTRADER_AGENT_HOST=127.0.0.1
# HEDGEEDGE_CTRADER_AGENT_PORT=5102
# HEDGEEDGE_CTRADER_AGENT_PATH=/path/to/ctrader-agent

# =============================================================================
# CODE SIGNING (For Distribution Builds)
# =============================================================================

# Windows Code Signing
# CSC_LINK=path/to/certificate.pfx
# CSC_KEY_PASSWORD=your-certificate-password

# macOS Code Signing & Notarization
# CSC_NAME=Developer ID Application: Your Name (TEAMID)
# APPLE_ID=your@apple.id
# APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
# APPLE_TEAM_ID=YOURTEAMID

# Skip notarization for local builds
# SKIP_NOTARIZE=true

# =============================================================================
# NOTES FOR DESKTOP BUILDS
# =============================================================================
# 1. These values are BAKED INTO the desktop app at build time
# 2. For security, never commit this file with real values to version control
# 3. The DESKTOP=1 env var is set automatically by electron:build scripts
# 4. Only VITE_ prefixed variables are available in the renderer process
# 5. Secrets (tokens, API keys) must NOT use the VITE_ prefix
# 6. Trading data flows via secure IPC - no HTTP CORS issues!
